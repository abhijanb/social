# Optional in dev — defaults to http://localhost:3000 when unset or empty.
# Must be a valid URL when set; trailing slashes are stripped.
# Example: VITE_API_URL=https://api.example.com
VITE_API_URL=http://localhost:3000
